# 独立域名部署

将本目录作为静态站点根目录部署到一个独立的公共或私有 HTTPS 域名。部署平台应支持：

- 静态文件与子目录路由；
- HTTPS；
- 飞书网页应用容器访问；
- 不注入限制嵌入的 `X-Frame-Options: DENY` 响应头。

发布后依次检查 `/`、`/ai/`、`/archive/`、`/data/reports.json`。然后将域名根地址配置为飞书网页应用主页地址。
